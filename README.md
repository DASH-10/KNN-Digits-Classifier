# KNN-Digits-Classifier
This project implements a KNN digit classifier from scratch on scikit-learn's `load_digits()` dataset, testing k values and L1/L2 distances and comparing against scikit-learn's KNN.
